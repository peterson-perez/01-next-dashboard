import { Metadata } from "next";

export const metadata: Metadata = {
    title: "customers"
};

const Costumers = () => {
    return (
        <div>
            <h2>Costumers</h2>
        </div>
    );
};

export default Costumers;